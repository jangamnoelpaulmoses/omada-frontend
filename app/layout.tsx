import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Omada - Instagram Profile Grading & Analytics',
  description: 'Your growth partner that thinks, acts, and keeps you in the loop. Grade your Instagram profile and get actionable insights to improve your social media presence.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
